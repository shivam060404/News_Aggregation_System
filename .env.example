# ============================================================================
# News Aggregation System - Environment Variables Configuration
# ============================================================================
# 
# This file contains all environment variables required to run the news
# aggregation system. Copy this file to .env and fill in your actual values.
#
# IMPORTANT: Never commit the .env file with real API keys to version control!
#
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED: News API Configuration
# ----------------------------------------------------------------------------
# API key for fetching news articles from NewsAPI.org
# 
# How to get your key:
#   1. Visit https://newsapi.org/register
#   2. Sign up for a free account (100 requests/day)
#   3. Copy your API key from the dashboard
#
# Example: NEWS_API_KEY=abc123def456ghi789jkl012mno345pqr678
NEWS_API_KEY=your_news_api_key_here

# ----------------------------------------------------------------------------
# REQUIRED: AI API Configuration
# ----------------------------------------------------------------------------
# API key for AI-powered article summarization
#
# Supported AI Providers:
#   - OpenAI (GPT models): https://platform.openai.com/api-keys
#   - Anthropic (Claude): https://console.anthropic.com/
#   - Google (Gemini): https://makersuite.google.com/app/apikey
#   - Groq (Fast inference): https://console.groq.com/
#
# Example: AI_API_KEY=sk-proj-abc123def456ghi789jkl012mno345pqr678
AI_API_KEY=your_ai_api_key_here

# ----------------------------------------------------------------------------
# OPTIONAL: AI Provider Selection
# ----------------------------------------------------------------------------
# Specify which AI provider to use for summarization
# Options: openai, claude, gemini, groq
# Default: openai
#
# Note: Make sure your AI_API_KEY matches the selected provider
AI_PROVIDER=openai

# ----------------------------------------------------------------------------
# OPTIONAL: Storage Configuration
# ----------------------------------------------------------------------------
# Choose how to store aggregated news articles
# Options:
#   - csv: Save to CSV file (simple, no database required)
#   - database: Save to relational database (PostgreSQL or SQLite)
#   - web_ui: Save with web interface (not yet implemented)
# Default: csv
STORAGE_TYPE=csv

# ----------------------------------------------------------------------------
# OPTIONAL: CSV Storage Configuration
# ----------------------------------------------------------------------------
# Path where CSV file will be saved (used when STORAGE_TYPE=csv)
# The directory will be created if it doesn't exist
# Default: output/articles.csv
OUTPUT_PATH=output/articles.csv

# ----------------------------------------------------------------------------
# OPTIONAL: Database Storage Configuration
# ----------------------------------------------------------------------------
# Database connection URL (required when STORAGE_TYPE=database)
#
# PostgreSQL format:
#   DATABASE_URL=postgresql://username:password@localhost:5432/database_name
#
# SQLite format (file-based, no server required):
#   DATABASE_URL=sqlite:///news.db
#   DATABASE_URL=sqlite:///path/to/database.db
#
# MySQL format:
#   DATABASE_URL=mysql://username:password@localhost:3306/database_name
#
# Leave empty if using CSV storage
DATABASE_URL=

# ============================================================================
# Additional Notes
# ============================================================================
#
# Command-Line Overrides:
#   All environment variables can be overridden using command-line arguments.
#   Example: python main.py --storage-type database --test-set 1
#
# Test Sets:
#   The system will prompt you to select a test set when running, or you can
#   specify it with --test-set flag:
#     1 = IT Services (TCS, Wipro, Infosys, HCLTech)
#     2 = Telecom (Airtel, Jio, Vodafone Idea, BSNL, MTNL, Tejas Networks)
#     3 = AI Companies (OpenAI, Anthropic, Google Deepmind, Microsoft, Meta)
#     4 = Tech Giants (Microsoft, Google, Apple, Meta)
#
# Security:
#   - Never commit .env file to version control
#   - Add .env to your .gitignore file
#   - Keep your API keys confidential
#   - Rotate keys if accidentally exposed
#
# Troubleshooting:
#   - Ensure no spaces around the = sign
#   - Don't use quotes around values unless they contain spaces
#   - Check that .env file is in the project root directory
#   - Restart the application after changing .env values
#
# ============================================================================
